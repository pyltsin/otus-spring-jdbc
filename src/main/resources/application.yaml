spring:
  data:
    mongodb:
      authentication-database: admin
      database: test
      host: localhost
      port: 27017
logging:
  level:
    org:
      hibernate:
        SQL: trace
        type: trace
        generate_statistics: trace
